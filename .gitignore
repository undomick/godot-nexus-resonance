# Godot 4+ specific ignores
.godot/
.nomedia

# Editor / IDE (local)
.vscode/
.vs/

# Local toolchain extracts
.tools/

# Local / relocated project trees
blender/
project/
references/
tests/
wiki/

# PowerShell artifact
$null

# Steam Audio SDK (fetched via scripts/install_steam_audio.py from Valve releases)
src/lib/steamaudio/

# SCons database
.sconsign.dblite
compile_commands.json

# Local runtime logs
logs/*
!logs/.gitkeep

# Build artifacts
build/
*.o
*.obj
# Transient OBJ+MTL staging for editor export (not C++ object files)
**/_nexus_obj_staging/
src/gen/

# GDExtension binaries (built by CI, distributed via GitHub Releases)
addons/nexus_resonance/bin/

# Affinity Designer lock files while a document is open (repo-root icons/ sources)
*~lock~
