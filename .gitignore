# Godot 4+ specific ignores
.godot/
.nomedia

# Local toolchain extracts (e.g. LLVM for clang-format matching CI)
.tools/

# Visual Studio
.vs/

# PowerShell artifact
$null

# Steam Audio SDK (fetched via scripts/install_steam_audio.py from Valve releases)
src/lib/steamaudio/

# SCons database
.sconsign.dblite

# Build artifacts
build/
*.o
*.obj
compile_commands.json
# Transient OBJ+MTL staging for editor export (not C++ object files)
**/_nexus_obj_staging/
src/gen/

# GDExtension binaries (built by CI, distributed via GitHub Releases)
audio_resonance_tool/addons/nexus_resonance/bin/

# GUT sidecar mesh test output (ResourceSaver)
audio_resonance_tool/test/unit/_gut_reso_sidecar/
