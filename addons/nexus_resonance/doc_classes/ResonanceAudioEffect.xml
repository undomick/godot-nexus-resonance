<?xml version="1.0" encoding="UTF-8" ?>
<class name="ResonanceAudioEffect" inherits="AudioEffect" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../class.xsd">
	<brief_description>
		Audio bus effect for reverb output. Auto-configured by ResonanceRuntime.
	</brief_description>
	<description>
		ResonanceAudioEffect is an [AudioEffect] that processes the reverb bus. It is added to [member ResonanceRuntimeConfig.reverb_bus_name] when the plugin or [url=#ResonanceRuntime]ResonanceRuntime[/url] initializes.
		For Convolution reflection type, it pulls accumulated Ambisonics wet from the Steam Audio reflection mixer, decodes it with the listener pose (same DSP tick as [code]iplReflectionMixerApply[/code]), and adds it to the bus input buffer (prior effects / sends on this bus).
		For Parametric/Hybrid, reverb is applied per-source in [ResonancePlayer]; when dry and wet buses differ, wet is routed to a separate child player on the effective reverb bus. With no reflection mixer, this effect passes bus audio through unchanged.
		Adjust [member gain_db] for reverb bus volume. After this bus, Godot routes output to the same target as [method ResonanceRuntimeConfig.get_bus_effective] (runtime [member ResonanceRuntimeConfig.bus]).
	</description>
	<tutorials>
	</tutorials>
	<members>
		<member name="gain_db" type="float" setter="set_gain_db" getter="get_gain_db" default="0.0">
			Volume correction in dB for the reverb bus.
		</member>
	</members>
</class>
